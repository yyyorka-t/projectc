The objective of this project was to create a C program that reads a text file, allows the user to input a word, and counts how many times that exact word appears in the file. An additional objective was to provide the user with an option to hide the specified word by replacing it with asterisks in a separate output file. The project aims to demonstrate basic text processing using fundamental C programming concepts such as file handling, string comparison, and loops
